sudo: required
language: node_js
node_js:
  - "7"
branches:
  only:
    - master
install:
  - npm install -g firebase-tools
  - npm install -g @angular/cli
  - npm install
script:
  - ng build --prod
after_success:
  - firebase deploy --token $FIREBASE_TOKEN